<div class="col-md-4">
    <!-- Blog Categories Well -->
    <div class="bar well">
        <h4>Tag cloud</h4>
        <div class="row">
            <div class="col-lg-12">
                <ul id="tagcloud">
                    <% @tags.each do |tag| %>
                        <li data-weight = <%= tag.taggings_count %> > <%= link_to tag.name, tasks_search_by_tag_path(current_locale, tag: tag.name), title: tag.taggings_count %> </li>
                    <% end %>
                </ul>
            </div>
        </div>
    </div>
</div>